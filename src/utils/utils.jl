
@doc """ check whether there are two gene whose location intersect
""" ->
function check_geneloc()
    chr_sted_gene = load(chr_sted_gene_fl, "chr_sted_gene_dict")
end

@doc """ all the chrsteds can't intersect with each other
""" ->
function check_chrsteds()

end
